let day = 10;

switch(day){
    case 0:
        console.log("sunday");
        break;

    default:
        console.log("invalid day");
}


// float cannot be used in the switch as well as in the case.
// cannot use variable expression in case.
// cannot use the relational operator in case.